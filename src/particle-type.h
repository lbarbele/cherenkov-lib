/**
 * @file
 * @brief Defines the enum ParticleType, which enumerates the particle types supported by the Cherenkov library.
 */

#pragma once

/**
 * @enum mapper::ParticleType
 * @brief Enumeration of primary particle types.
 */
enum class ParticleType {
	Proton,
	Gamma
};
